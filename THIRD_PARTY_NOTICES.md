# Third-Party Notices

VoicePlus includes the `sherpa-onnx` WebAssembly runtime, distributed under the Apache License 2.0.

VoicePlus includes `@picovoice/pvrecorder-node`, distributed under the Apache License 2.0, for local microphone capture.

The optional first-use speech model is downloaded from the `k2-fsa/sherpa-onnx` `asr-models` GitHub release. Before distributing VoicePlus beyond the private repository, review and preserve the model's accompanying license and attribution files from the extracted archive.

VoicePlus does not bundle the downloaded speech model inside its VSIX.